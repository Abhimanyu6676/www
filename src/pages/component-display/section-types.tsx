import React from "react"
import SectionType1Sample from "../../components/common/sectionType1/sample"

type Props = {}

const SectionTypes = (props: Props) => {
  return (
    <div>
      <SectionType1Sample />
    </div>
  )
}

export default SectionTypes
